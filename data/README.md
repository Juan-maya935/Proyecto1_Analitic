# data/

Aquí va `datos_proyecto_1.zip` (17,8 MB) tal como lo entregó el profesor.
No se versiona por peso; cada quien lo descarga de Moodle y lo descomprime:

```bash
unzip data/datos_proyecto_1.zip -d data/
# → data/datos_proyecto_1/imagenes_semanales/...
```

Los `.tif` descomprimidos están en `.gitignore`.
